int main(){
    float f = .0f;
    return (1 / 5 --- (4));
}

#include <fstream>
#include "TokenStorage.h"

using namespace std;

class MyClass final
{
public:
    MyClass(int a, int b)
    {
        if (true)
        {
        }
        else
        {
        }

        float temp;
    };
    /*
    float fl = 1.0;
    */

    virtual void Function() = 0;

protected:

    int Temp()
    {
        int i = 0;
        while (i <= 5)
        {
            i++;
        }

        for (int i = 0; i < 5; i++)
        {

        }

        // comment
        return 1 || 2 && 3 * 5 / 1; int c = 5;
    }
private:

    int m_identifier;
};


int main()
{
    ifstream input("input.txt");
    //ofstream output("output.txt");
}
